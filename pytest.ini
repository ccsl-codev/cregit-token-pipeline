[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
# -q keeps the log short enough to paste into a review.
# --strict-markers stops a typo in a marker name from silently skipping a test.
addopts = -q --strict-markers
markers =
    network: needs a live network or an authenticated gh (deselected by default)
